package dct;

import org.openrdf.annotations.Iri;
import rdfs.comment;
import rdfs.isDefinedBy;
import rdfs.label;
import skos.definition;

/** A method by which resources are added to a collection. */
@label({"Method of Accrual", "Method of Accrual"})
@definition({"A method by which resources are added to a collection."})
@comment({"A method by which resources are added to a collection."})
@isDefinedBy({"http://purl.org/dc/terms/"})
@Iri("http://purl.org/dc/terms/MethodOfAccrual")
public interface MethodOfAccrual {
}
