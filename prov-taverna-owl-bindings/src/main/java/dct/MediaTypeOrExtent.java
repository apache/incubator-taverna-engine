package dct;

import org.openrdf.annotations.Iri;
import rdfs.comment;
import rdfs.isDefinedBy;
import rdfs.label;
import skos.definition;

/** A media type or extent. */
@label({"Media Type or Extent", "Media Type or Extent"})
@definition({"A media type or extent."})
@comment({"A media type or extent."})
@isDefinedBy({"http://purl.org/dc/terms/"})
@Iri("http://purl.org/dc/terms/MediaTypeOrExtent")
public interface MediaTypeOrExtent {
}
